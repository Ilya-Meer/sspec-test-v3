## 1. Word List Module

- [ ] 1.1 Create `words.js` with a static array of at least 50 words (mix of nouns and adjectives)
- [ ] 1.2 Implement `getRandomPhrase()` that picks 2 distinct words at random and returns them joined with a space

## 2. HTML Structure

- [ ] 2.1 Add `<span id="flavour-text"></span>` as the first child inside `<main class="container">` in `index.html`

## 3. Styling

- [ ] 3.1 Add CSS for `#flavour-text` with font-size 0.75rem, color `var(--color-muted)`, and appropriate spacing

## 4. Wiring

- [ ] 4.1 Import `getRandomPhrase` from `words.js` in `app.js` and set `#flavour-text` text content on page load
